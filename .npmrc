# Keep these compatibility flags for now: the current dependency graph still
# relies on broad peer resolution and hoisting behavior. Revisit after aligning
# PrimeVue/Vite/Tauri-era peer ranges so installs can move back to stricter pnpm.
shamefully-hoist=true
strict-peer-dependencies=false

minimum-release-age=10080
